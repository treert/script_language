lua支持块作用域和闭包。
典型官方例子
```
     a = {}
     local x = 20
     for i=1,10 do
       local y = 0
       a[i] = function () y=y+1; return x+y end
     end
```
创建了十个闭包【函数】,每个闭包拥有不同的y和相同的x。

## 疑问
未声明的变量直接使用，先查闭包链，最后是全局变量。逻辑上这样，运行时就不划算了。
这个有些奇怪呀，是编译时把就确定索引了吗！